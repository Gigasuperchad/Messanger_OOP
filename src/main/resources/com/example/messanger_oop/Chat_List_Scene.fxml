<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.messanger_oop.HelloController"
      prefHeight="650.0"
      prefWidth="400.0"
      spacing="15.0"
      style="-fx-background-color: linear-gradient(to bottom, #667eea 0%, #764ba2 100%);">

    <padding>
        <Insets top="20.0" right="20.0" bottom="20.0" left="20.0"/>
    </padding>

    <!-- Заголовок -->
    <Label text="💬 Мои чаты"
           style="-fx-font-weight: bold; -fx-font-size: 24; -fx-text-fill: white; -fx-padding: 0 0 10 0;"/>

    <!-- Список чатов -->
    <ListView fx:id="Chat_list"
              VBox.vgrow="ALWAYS"
              style="-fx-background-color: white; -fx-background-radius: 15; -fx-border-radius: 15; -fx-padding: 5; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 5);"/>

    <!-- Первая строка кнопок -->
    <HBox spacing="10.0" style="-fx-padding: 10 0;">
        <Button mnemonicParsing="false" onAction="#handleCreateChat"
                prefHeight="45.0" prefWidth="190.0" text="➕ Создать чат"
                style="-fx-background-color: linear-gradient(to right, #4CAF50, #8BC34A); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 25; -fx-font-size: 14;"/>

        <Button fx:id="deleteChatButton" mnemonicParsing="false" onAction="#handleDeleteChat"
                prefHeight="45.0" prefWidth="190.0" text="🗑️ Удалить чат" disable="true"
                style="-fx-background-color: linear-gradient(to right, #f44336, #FF5252); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 25; -fx-font-size: 14;"/>
    </HBox>

    <!-- Вторая строка кнопок -->
    <HBox spacing="10.0" style="-fx-padding: 0 0 10 0;">
        <Button mnemonicParsing="false" onAction="#handleStatusButton"
                prefHeight="45.0" prefWidth="190.0" text="🟢 Статус"
                style="-fx-background-color: linear-gradient(to right, #FF9800, #FFB74D); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 25; -fx-font-size: 14;"/>

        <Button mnemonicParsing="false" onAction="#handleProfileButton"
                prefHeight="45.0" prefWidth="190.0" text="👤 Мой профиль"
                style="-fx-background-color: linear-gradient(to right, #2196F3, #03A9F4); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 25; -fx-font-size: 14;"/>
    </HBox>

    <!-- Третья строка кнопок -->
    <HBox spacing="10.0">
        <Button mnemonicParsing="false" onAction="#handleLogout"
                prefHeight="45.0" prefWidth="400.0" text="🚪 Выйти"
                style="-fx-background-color: linear-gradient(to right, #757575, #9E9E9E); -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 25; -fx-font-size: 14;" />
    </HBox>
</VBox>