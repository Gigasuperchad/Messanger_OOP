<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox prefHeight="650.0" prefWidth="400.0" spacing="15.0"
      style="-fx-background-color: linear-gradient(to bottom, #667eea 0%, #764ba2 100%);"
      xmlns="http://javafx.com/javafx/17.0.12"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.messanger_oop.HelloController">

    <padding>
        <Insets bottom="25.0" left="25.0" right="25.0" top="25.0" />
    </padding>

    <!-- Заголовок -->
    <Label style="-fx-font-weight: bold; -fx-font-size: 26; -fx-text-fill: white;"
           text="💬 Мои чаты" />

    <!-- Счетчик чатов -->
    <Label fx:id="chatCountLabel"
           style="-fx-font-size: 14px; -fx-text-fill: rgba(255,255,255,0.8);"
           text="0 чатов" />

    <!-- Карточка для списка чатов - ОСНОВНОЙ КОНТЕЙНЕР -->
    <VBox fx:id="chatListContainer"
          style="-fx-background-color: white; -fx-background-radius: 10; -fx-padding: 10; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 5, 0, 0, 2);"
          VBox.vgrow="ALWAYS">

        <!-- Список чатов - ЗАНИМАЕТ ВСЁ ПРОСТРАНСТВО -->
        <ListView fx:id="Chat_list"
                  style="-fx-background-color: transparent; -fx-background-insets: 0; -fx-padding: 0; -fx-border-width: 0; -fx-border-radius: 5;"
                  VBox.vgrow="ALWAYS" />

        <!-- Подсказка при отсутствии чатов -->
        <VBox fx:id="emptyChatsHint" alignment="CENTER" spacing="10"
              style="-fx-padding: 40 20;" visible="false">
            <Label style="-fx-font-size: 48; -fx-text-fill: #ccc;" text="💬" />
            <Label style="-fx-font-size: 14; -fx-text-fill: #888; -fx-text-alignment: center;"
                   text="У вас пока нет чатов\nСоздайте первый чат!"
                   wrapText="true" />
            <Button mnemonicParsing="false" onAction="#handleCreateChat"
                    style="-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"
                    text="➕ Создать чат" />
        </VBox>
    </VBox>

    <!-- Кнопки -->
    <HBox spacing="12.0">
        <Button mnemonicParsing="false" onAction="#handleCreateChat"
                prefHeight="48.0" prefWidth="194.0"
                style="-fx-background-color: #4CAF50; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"
                text="➕ Создать чат" />

        <Button fx:id="deleteChatButton" disable="true"
                mnemonicParsing="false" onAction="#handleDeleteChat"
                prefHeight="48.0" prefWidth="194.0"
                style="-fx-background-color: #ff4444; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 20; -fx-border-color: #ff7777; -fx-border-width: 2; -fx-cursor: hand;"
                text="Удалить" />
    </HBox>

    <HBox spacing="12.0">
        <Button mnemonicParsing="false" onAction="#handleProfileButton"
                prefHeight="48.0" prefWidth="194.0"
                style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"
                text="👤 Профиль" />

        <Button fx:id="infoButton" mnemonicParsing="false" onAction="#handleOpenChatInfo"
                prefHeight="48.0" prefWidth="194.0"
                style="-fx-background-color: #FFC107; -fx-text-fill: #333333; -fx-font-weight: bold; -fx-background-radius: 20; -fx-border-color: #FFD54F; -fx-border-width: 2; -fx-cursor: hand; -fx-opacity: 1.0;"
                text="Инфо" />
    </HBox>

    <Button mnemonicParsing="false" onAction="#handleLogout"
            prefHeight="48.0" prefWidth="400.0"
            style="-fx-background-color: #757575; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 10; -fx-cursor: hand;"
            text="Выйти" />
</VBox>