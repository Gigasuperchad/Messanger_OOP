<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>
<?import javafx.scene.image.Image?>

<ScrollPane fitToWidth="true" style="-fx-background-color: transparent; -fx-border-color: transparent;"
            xmlns="http://javafx.com/javafx/17.0.2-ea"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.messanger_oop.LoginController">

    <AnchorPane prefHeight="700.0" prefWidth="500.0"
                style="-fx-background-color: linear-gradient(to bottom right, #667eea 0%, #764ba2 100%);">

        <!-- Главный контейнер -->
        <VBox alignment="CENTER" spacing="30"
              AnchorPane.topAnchor="40.0"
              AnchorPane.bottomAnchor="40.0"
              AnchorPane.leftAnchor="40.0"
              AnchorPane.rightAnchor="40.0">

            <!-- Логотип и заголовок -->
            <VBox alignment="CENTER" spacing="15">
                <ImageView fitHeight="80" fitWidth="80">
                    <image>
                        <Image url="@messenger-icon.png" />
                    </image>
                </ImageView>
                <Label text="Вход в мессенджер"
                       style="-fx-text-fill: white; -fx-font-size: 32; -fx-font-weight: bold;"/>
                <Label text="Соединяем людей (буквально)"
                       style="-fx-text-fill: rgba(255,255,255,0.8); -fx-font-size: 16;"/>
            </VBox>

            <!-- Карточка формы -->
            <VBox alignment="CENTER" spacing="25"
                  style="-fx-background-color: white; -fx-background-radius: 20; -fx-padding: 40; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.2), 20, 0, 0, 10);"
                  maxWidth="400">

                <Label text="Введите ваши данные"
                       style="-fx-text-fill: #333; -fx-font-size: 24; -fx-font-weight: bold;"/>

                <!-- Поля ввода -->
                <VBox spacing="15" prefWidth="300">
                    <TextField fx:id="usernameField"
                               prefHeight="50"
                               promptText="Логин"
                               style="-fx-background-color: #f8f9fa; -fx-border-color: #e0e0e0; -fx-border-radius: 10; -fx-padding: 0 15; -fx-font-size: 14;"/>

                    <PasswordField fx:id="passwordField"
                                   prefHeight="50"
                                   promptText="Пароль"
                                   style="-fx-background-color: #f8f9fa; -fx-border-color: #e0e0e0; -fx-border-radius: 10; -fx-padding: 0 15; -fx-font-size: 14;"/>

                    <Label fx:id="statusLabel"
                           prefHeight="25"
                           style="-fx-text-fill: #f44336; -fx-font-size: 13; -fx-padding: 5 0;"/>

                    <!-- Кнопки -->
                    <VBox spacing="10">
                        <Button fx:id="loginButton"
                                text="🔓 Войти"
                                prefHeight="50"
                                style="-fx-background-color: linear-gradient(to right, #4CAF50, #8BC34A); -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 16; -fx-background-radius: 25;"/>

                        <Button fx:id="registerButton"
                                text="📝 Регистрация"
                                prefHeight="50"
                                style="-fx-background-color: transparent; -fx-text-fill: #667eea; -fx-font-weight: bold; -fx-border-color: #667eea; -fx-border-width: 2; -fx-border-radius: 25; -fx-font-size: 16;"/>
                    </VBox>
                </VBox>
            </VBox>

            <!-- Информация о профиле (скрыта по умолчанию) -->
            <VBox fx:id="profileInfoBox"
                  alignment="CENTER"
                  spacing="15"
                  style="-fx-background-color: white; -fx-background-radius: 15; -fx-padding: 20; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, 5);"
                  maxWidth="400"
                  visible="false">

                <Label text="👤 Профиль пользователя"
                       style="-fx-text-fill: #333; -fx-font-size: 18; -fx-font-weight: bold;"/>

                <HBox spacing="20" alignment="CENTER_LEFT">
                    <ImageView fx:id="avatarImageView"
                               fitHeight="60" fitWidth="60"
                               style="-fx-background-radius: 30; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 5, 0, 0, 2);">
                        <image>
                            <Image url="@default-avatar.png" />
                        </image>
                    </ImageView>

                    <VBox spacing="8">
                        <HBox spacing="5">
                            <Label text="Имя:" style="-fx-font-weight: bold; -fx-text-fill: #555;"/>
                            <Label fx:id="firstNameLabel" text="" style="-fx-text-fill: #333;"/>
                        </HBox>
                        <HBox spacing="5">
                            <Label text="Email:" style="-fx-font-weight: bold; -fx-text-fill: #555;"/>
                            <Label fx:id="emailLabel" text="" style="-fx-text-fill: #333;"/>
                        </HBox>
                    </VBox>
                </HBox>

                <Label text="Нажмите 'Войти' чтобы продолжить"
                       style="-fx-text-fill: #666; -fx-font-size: 13;"/>
            </VBox>

            <!-- Футер -->
        </VBox>
    </AnchorPane>
</ScrollPane>